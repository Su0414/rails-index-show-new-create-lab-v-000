<h1>Coupon Index</h1>
<ul>
<% @coupons.each do |c| %>
  <li><%= link_to c.coupon_code, coupon_path(c) %></li>
<% end %>
</ul>


<div>
  <% @coupons.each do |c| %>
    <div><%= link_to c.coupon_code, coupon_path(c) %></div>
  <% end %>
</div>
